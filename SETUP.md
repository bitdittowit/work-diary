# Быстрая настройка

## 1. Сделайте скрипт исполняемым

```bash
chmod +x scripts/work-diary.sh
```

## 2. Добавьте алиас в ваш shell

Откройте `~/.zshrc` (или `~/.bash_profile` если используете bash):

```bash
nano ~/.zshrc
```

Добавьте в конец файла (замените путь на ваш):

```bash
# Work Diary alias
alias diary="/path/to/work-diary/scripts/work-diary.sh"
```

Например, если проект находится в `~/projects/work-diary`:
```bash
alias diary="$HOME/projects/work-diary/scripts/work-diary.sh"
```

Примените изменения:

```bash
source ~/.zshrc
```

## 3. Используйте дневник

См. список команд в [README.md](README.md).

## 4. Настройте напоминания (опционально)

Для ежедневных напоминаний в рабочие дни (пн-пт):

```bash
./scripts/reminder.sh setup
```

Скрипт спросит время напоминания (по умолчанию 18:00), затем активируйте:

```bash
launchctl load ~/Library/LaunchAgents/com.workdiary.reminder.plist
```

**Управление напоминаниями:**
```bash
./scripts/reminder.sh show    # Показать текущее время
./scripts/reminder.sh remove  # Удалить напоминание
./scripts/reminder.sh setup   # Настроить новое
```

**Важно:** Напоминания будут приходить только в рабочие дни (понедельник-пятница).

## 5. Настройте редактор (опционально)

Если хотите использовать VS Code вместо nano:

```bash
echo 'export DIARY_EDITOR="code"' >> ~/.zshrc
source ~/.zshrc
```

Или Vim:

```bash
echo 'export DIARY_EDITOR="vim"' >> ~/.zshrc
source ~/.zshrc
```

## Структура файлов

См. описание структуры в [README.md](README.md).

## Готово! 🎉

Теперь просто используйте `diary` в терминале для работы с дневником.

**Совет:** Настройте напоминания, чтобы не забывать вести дневник каждый рабочий день!
