# Описание скриптов проекта

Этот документ объясняет назначение каждого скрипта в проекте.

> 📁 Все скрипты находятся в папке `scripts/`

## Основные скрипты

### `work-diary.sh` - Главный скрипт дневника
**Назначение:** Основной скрипт для работы с дневником.

**Что делает:**
- Создает новые записи на день
- Открывает существующие записи
- Показывает список всех записей (сгруппированные по спринтам)
- Ищет по записям
- Показывает статистику

**Использование:**
```bash
./scripts/work-diary.sh [new|open|list|search|stats]
# или через алиас
diary [new|open|list|search|stats]
```

**Зависимости:** Нет

---

## Скрипты для напоминаний

### `work-diary-reminder.sh` - Обертка для LaunchAgent
**Назначение:** Промежуточный скрипт, который вызывается системой macOS через LaunchAgent.

**Что делает:**
- Проверяет, рабочий ли сегодня день (пн-пт)
- Если рабочий день → показывает системное уведомление macOS с звуком и вызывает `work-diary.sh open`
- Если выходной → ничего не делает

**Использование:** 
Не вызывается напрямую пользователем. Используется системой macOS через LaunchAgent.

**Зависимости:** `work-diary.sh`

**Почему нужен:** LaunchAgent не может напрямую вызывать `work-diary.sh open`, потому что нужно проверить рабочий день. Этот скрипт делает эту проверку и показывает явное уведомление.

---

### `reminder.sh` - Управление напоминаниями
**Назначение:** Полнофункциональный интерфейс для управления напоминаниями.

**Что делает:**
- Показывает текущее время напоминания и статус
- Удаляет существующее напоминание
- Настраивает новое напоминание (создает plist файл)

**Использование:**
```bash
./scripts/reminder.sh show    # Показать текущее время
./scripts/reminder.sh remove  # Удалить напоминание
./scripts/reminder.sh setup   # Настроить новое
```

**Зависимости:** `work-diary-reminder.sh`

**Почему нужен:** Предоставляет удобный интерфейс для управления напоминаниями вместо ручного редактирования plist файлов.

---

## Схема работы напоминаний

```
Пользователь → scripts/reminder.sh setup
                    ↓
            reminder.sh создает plist файл
                    ↓
            LaunchAgent (система macOS)
                    ↓
            scripts/work-diary-reminder.sh (проверяет рабочий день)
                    ↓
            scripts/work-diary.sh open (открывает дневник)
```

## Рекомендации по использованию

- **Для работы с дневником:** используйте `scripts/work-diary.sh` (или алиас `diary`)
- **Для управления напоминаниями:** используйте `scripts/reminder.sh`
